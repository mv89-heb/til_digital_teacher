# Local development
FLASK_ENV=development
DATABASE_URL=postgresql://user:password@localhost/til_db
JWT_SECRET=change-me-locally
JWT_EXPIRES_DAYS=7
CORS_ORIGINS=http://localhost:3000

# Render + Neon production
# FLASK_ENV=production
# DATABASE_URL=postgresql://<neon-user>:<password>@<neon-host>/<database>?sslmode=require
# JWT_SECRET=<at-least-32-random-characters>
# JWT_EXPIRES_DAYS=1
# CORS_ORIGINS=https://til-digital-teacher-0ryl.onrender.com
# MAX_CONTENT_LENGTH=2097152
# RATELIMIT_STORAGE_URI=redis://<shared-redis-host>:6379/0
